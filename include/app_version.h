/*
 * Application version for MCUboot image signing and display.
 * Format: major.minor.patch (e.g. 1.0.0)
 * Bump this before each release so MCUboot installs OTA updates correctly.
 */
#ifndef APP_VERSION_H
#define APP_VERSION_H

#define APP_VERSION "2.2.0"  /* v2.2.x : Major refactoring + all features from v2.0.x */

#endif /* APP_VERSION_H */
